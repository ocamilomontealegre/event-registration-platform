// Importing the MainContainer component from the './components/containers/MainContainer.jsx' file
import { MainContainer } from './components/containers/MainContainer.jsx';
// Importing CSS file for styling
import './App.css';

// Defining the root component of the application
export const App = () => {
  // Rendering the MainContainer component
  return (
    <MainContainer />
  );
};